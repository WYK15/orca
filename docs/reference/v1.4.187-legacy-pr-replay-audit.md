# v1.4.187 Legacy PR Replay Audit

## Scope

This record compares selected downstream commits from `origin/custom/main` with `sync/v1.4.187-wyk.1`. It uses each source commit's zero-context diff hunks, not commit titles or repository-wide file scans.

A source commit may contain hunks owned by more than one customization. `902e1356c` is the canonical example: terminal-scan hunks belong to ORCAW-010 and AI Vault locale hunks belong to ORCAW-009.

## Dispositions

| ID | Legacy source commits | Result | Evidence / notes |
| --- | --- | --- | --- |
| ORCAW-001 | `4f5072d6a`, `c0419401a`, `7e7b4f6eb`, `b27105fa7`, `a4de39535`, `e493efa52`, `57369e437` | bounded by identity contract | Product identity, updater, CLI, helper identity, and package checks were migrated. The old range has broad historical test-path drift; retain focused identity verification rather than infer exact textual equivalence. |
| ORCAW-002 | `5bf180c44`, `442656e5c`, `088549f31`, `b1b37099d`, `6ba166bce`, `108641ed7`, `891bf2e24`, `6eae048f8` | bounded by release contract | Fork package publishing, disabled inherited schedules, signing, and release policy were migrated. Historical docs and obsolete updater tests are intentionally not replayed. |
| ORCAW-003 | `74a2a6501` | replayed | The 72px tab behavior is present. Only the historical planning document is absent. |
| ORCAW-004 | `b35adc13d` through `f1a77067f` | structurally replayed | Insert grid, custom sizing, resize, table actions, context routing, and tests are present. Old `RichMarkdownTableToolbar` and main-window menu paths were split into current controls/actions/routing modules. |
| ORCAW-005 | `e54a36947` through `86535139f` | structurally replayed | Current parser, character decoder, style parser, raw HTML encoder, extension, and node view cover the old safe-HTML behavior. Unmatched source lines are implementation-detail refactors. |
| ORCAW-006 | `06aeb2e98`, `f575a1992`, `b0e49ce60` | structurally replayed | `resolveBrowserPageTitleEvent` is now consumed by `host-guest/browser-page-webview-navigation-handlers.ts`; the old `BrowserPane` entry point no longer owns navigation. |
| ORCAW-007 | `d1badbb17`, `5830bd4f9` | structurally replayed | Old `RichMarkdownTableToolbar` Chinese labels are now `RichMarkdownTableControls` labels. |
| ORCAW-008 | `35a7ba420`, `4b6924df6`, `fecc10a9f` | structurally replayed | Current runtime returns without installation when no agents are detected and preserves explicit `unreadable` config state. Provider hook files were reorganized. |
| ORCAW-009 | `03a883948`, `64954a1cc`, locale hunks in `902e1356c` | structurally replayed | Codex delete discovery is now in `session-scanner-agent-sources.ts`; dialog and virtual row behavior use current confirmation-dialog and virtual-list structures. The 18 English/Chinese locale strings and session-title aria label are replayed in the ORCAW-009 commit. |
| ORCAW-010 | terminal-scan hunks in `902e1356c` | replayed | Current worktree scanning propagates operational failures and treats WSL timeout results as non-authoritative. Current evidence paths: `worktree.ts`, `worktree-scan-cache-sharing.test.ts`, `worktree-graph-listing.test.ts`, and `worktrees-detected-scan-cache.test.ts`. |
| ORCAW-011 | `110b7d33e` | replayed | All non-comment source additions are present. |
| ORCAW-012 | `3207dad8d` | structurally replayed | Remote parse cache, metadata invalidation, and fixture read tracking are present; the old scanner test fixture was split into `remote-session-scanner-test-fixtures.ts`. |
| ORCAW-013 | `84560ffd3` | structurally replayed | Windows path identity and active-tab reconciliation now live in `editor-tab-file-identity.test.ts` and `tabs-model-reconciliation.test.ts`. |
| ORCAW-014 | `aa27662b2` | replayed | All non-comment source additions are present. |
| ORCAW-015 | fork governance foundation | replayed | Registry, coverage, release contract, manual upstream workflow, and operations reference are present. |

## Required Follow-up

1. Before the next stable-upstream adoption, create an audit entry for each selected legacy PR range and assign every hunk a disposition with evidence.
2. For structural rewrites, record the replacement module and focused verification in this document instead of marking a path-only match as equivalent.
3. Keep historical plans, docs, and obsolete test locations explicitly marked as intentionally not replayed.
