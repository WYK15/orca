# v1.4.192 Customization Replay Audit

## Scope

This audit covers the candidate range `origin/upstream-sync..HEAD` for the
v1.4.192 adoption. The 16 `FORK_NOTES.md` entries are the review units. Every
active and upstream-candidate entry is replayed once; none is retired.

## Replay dispositions

| ID | Disposition | Evidence |
| --- | --- | --- |
| ORCAW-001 | replayed | Orcaw identity, isolated data, CLI/helper names, and manual release delivery were ported through the current packaging, CLI, startup, updater, and UpdateCard seams. CLI, WSL, identity, updater-policy, and UpdateCard tests passed. |
| ORCAW-002 | replayed | Fork package delivery was ported with Developer ID signing and notarization. The earlier signing recovery is folded into this replay. Inherited schedules, including Node-next compatibility, are disabled. Desktop-package, macOS release-environment, release-contract, publication workflow, and trigger-policy tests passed. |
| ORCAW-003 | replayed | The 72px tab compression rule and focused tab test were retained. |
| ORCAW-004 | replayed | Table insertion and sizing controls remain replayed; structural UI hunks remain covered by their focused tests. |
| ORCAW-005 | replayed | Safe HTML source and node-view behavior remain replayed. |
| ORCAW-006 | replayed | Stale browser-title events and active-worktree title behavior remain replayed through the current browser navigation seam. |
| ORCAW-007 | replayed | Chinese editor and menu labels remain replayed with the localization test. |
| ORCAW-008 | replayed | Hook configuration remains fail-closed for missing or unreadable input across local, WSL, and remote provider paths. |
| ORCAW-009 | replayed | Codex discovery/deletion and bulk AI Vault deletion remain replayed through current IPC and renderer seams. |
| ORCAW-010 | replayed | Failed WSL scans remain non-authoritative and preserve existing terminals. |
| ORCAW-011 | replayed | Linux relay process scans remain bounded. |
| ORCAW-012 | replayed | Remote transcript parse-cache behavior is replayed; `remote-session-scanner.test.ts` passed 21/21. |
| ORCAW-013 | replayed | Windows-normalized editor file identity remains replayed with current tab reconciliation. |
| ORCAW-014 | replayed | Markdown source editing, table of contents, and inline editing were ported to the current editor lifecycle. Focused lifecycle, table-of-contents, and inline-edit tests passed. |
| ORCAW-015 | replayed | Registry, coverage, release, policy-conflict, and controlled upstream-tracking governance are consolidated into one trailer-bearing commit. The fixture allowlist, legacy audit, policy evidence, archive/lease tracking corrections, and environment-specific Vitest setup are folded here. Governance and release contract tests passed 21/21. |
| ORCAW-016 | replayed | Non-destructive project removal, distinct permanent child-worktree deletion, and confirmation were ported through the current sidebar lifecycle. Focused context-menu/delete suite passed 60/60. |

## Folded and dropped source units

| Source unit | Disposition |
| --- | --- |
| `76095d516` macOS signing recovery | Folded into ORCAW-002. |
| `99da39d62c` fixture security allowlist | Folded into ORCAW-015 because it owns the fork registry root-entry rule and `.gitleaksignore`. |
| `3cbf022d7`, `5af626604`, `c1e4b588`, `ac4b43fe` | Folded into ORCAW-015. |
| `03bdb859` source commit | Replaced by the trailer-bearing ORCAW-016 replay commit; the untrailed source commit is not retained. |
| `7ff96d5fe` v1.4.187 release commit | Dropped. The candidate has no release-version commit yet. |

## Observed issues and corrective actions

| Issue | Corrective action | Preventive control |
| --- | --- | --- |
| The old release commit survived the first replay. | Dropped it before candidate validation. | Create the release-version commit only after `main` replacement. |
| Replayed maintenance commits lacked or duplicated customization ownership. | Folded them into their owning ORCAW-002, -015, or -016 replay commits. | Run exact-one trailer coverage before release preparation. |
| A Node-only Vitest setup was loaded in happy-dom. | Removed it from global browser setup and installed the fake host explicitly in the Node CLI suite. | Validate changed test harnesses with one Node suite and one browser suite. |
| The Node-next inherited schedule remained enabled despite the fork policy. | Removed the schedule before `main` replacement. | Audit workflow triggers directly; do not rely solely on workflow tests. |
| Local release builds lacked protected signing credentials. | Used the tag-triggered Actions workflow, where signing and notarization secrets are available. | Reserve signed release builds for the protected workflow. |

## Coverage and remaining validation

The candidate passes registry validation and exact-one trailer coverage for all
16 entries. No customization is recorded as upstream-equivalent or retired.

After the candidate's functionality and governance are accepted, the
remaining release gates are the lease-protected `main` replacement, a
v1.4.192 release-version commit on `main`, and package artifact verification.
